# Upstream attribution

This Cloudflare mail module is based on the Apache-2.0 licensed `idinging/freemail` project. The original license is preserved in `LICENSE`.

The repository package intentionally excludes screenshots, deployment-specific IDs, domains, passwords, tokens, and the original `wrangler.toml` instance configuration.
